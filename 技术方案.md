# 桌面盒子（DesktopBoxes）— 技术方案文档

> 状态：MVP 已实现，正在稳定化与真机回归
> 应用名称：**桌面盒子**（英文名 **DesktopBoxes**）
> 目标：一个 Windows 桌面整理工具 —— 把应用/文件拖进「盒子」里，盒子可折叠成标题栏。

---

## 1. 项目目标与范围

做一个 Windows 桌面应用：在桌面上创建若干「盒子」（分区/容器），用户把应用、快捷方式、文件夹、文件拖进盒子，盒子可以**手动折叠成一条标题栏**、再展开，用来整理桌面。

### 已确认的需求决策（与用户对齐）

| 决策点 | 结论 |
| --- | --- |
| 目标平台 | **仅 Windows**（Win10 / Win11） |
| 盒子与桌面图标的关系 | **悬浮盒子挂件**：保留系统桌面图标，盒子作为贴桌面的可折叠浮层 |
| 技术栈 | 由方案推荐（本方案推荐 **C# / .NET 8 + WPF**） |
| 盒子内容来源 | **手动拖入为主**（.exe / .lnk / 文件夹 / 文件） |
| 折叠形态 | **折叠成标题栏**（显示盒子名 + 图标数量） |

### 不在本期范围（留作后续可选）

- 接管/隐藏系统桌面图标（「接管桌面」模式）
- 按规则自动归类桌面图标
- 文件夹门户（把文件夹映射为一个盒子实时显示内容）
- 跨平台（macOS / Linux）

---

## 2. 同类产品与实现调研

| 名称 | 定位 | 技术栈（已知/推断） | 参考价值 |
| --- | --- | --- | --- |
| Stardock Fences | 商业桌面分区工具 | 闭源 | 交互与视觉参考 |
| [desktop_box](https://github.com/kof2000git/desktop_box) | 开源 Fences 替代，标签页盒子、拖放、右键原生菜单、隐藏桌面图标 | **.NET 8 + WPF，单文件 exe** | **最直接的架构参考** |
| [openFrence](https://github.com/weiweigogo/openFrence) | 轻量 Windows 桌面图标分组工具 | C# 系 | 图标分组/贴桌面参考 |

结论：**成熟开源实现几乎都选择原生 C#/WPF 栈**，原因一致 —— 这类应用需要大量 Win32 桌面层操作（贴桌面、Shell 图标、.lnk 解析、OLE 拖放），原生栈最直接、最稳、体积最小。

---

## 3. 核心技术难点

本应用最「难」也最关键的，不是 UI，而是 **「贴桌面」**：盒子要表现成桌面上的一部分 —— 显示在壁纸之上、所有普通应用窗口之下，按 `Win+D` 显示桌面时仍可见，且不进入任务栏 / Alt-Tab。

Windows 桌面层级（自下而上）：

```
Progman（桌面根窗口）
├─ WorkerW（可由程序创建/复用的中间层窗口，壁纸引擎即利用此层）
└─ SHELLDLL_DefView → SysListView32（系统桌面图标列表）
（再之上是普通应用窗口，最上是置顶窗口）
```

**实现「贴桌面」的两种思路：**

1. **挂接桌面层（推荐，主方案）**：创建/复用一个 `WorkerW` 窗口，让无边框盒子窗口归其所有（位于壁纸之上、普通窗口之下），同时保留顶层窗口以使用 WPF 逐像素透明。优点：盒子背景和标题栏可半透明，图标与文字仍保持清晰；`Win+D` 后仍在，也不进入任务栏/Alt-Tab。缺点：WorkerW 层级在不同 Win10/Win11 版本有细微差异，需要做版本验证。
2. **置底降级方案**：普通顶层无边框窗口 + `SetWindowPos(HWND_BOTTOM)` 锁 z-order + 处理 `Show Desktop`/`WM_MOUSEACTIVATE` + `WS_EX_TOOLWINDOW` + `ITaskbarList::DeleteTab` 隐藏任务栏项。优点：不依赖桌面层结构。缺点：「贴桌面」是模拟的，在某些场景（如被其他窗口触发 z-order 变化）可能被顶起，体验略打折扣。

> 设计决定：**以方案 1 为主，方案 2 作为降级/兜底**。通过 **spike（技术验证原型）** 持续验证 WorkerW 挂接、透明度与 `Win+D` 行为。

---

## 4. 技术栈选型

| 维度 | **C# (.NET 8) + WPF（推荐）** | Electron | Tauri (Rust) |
| --- | --- | --- | --- |
| 贴桌面/窗口层级 | 直接 P/Invoke，原生支持，最稳 | 需写 Node 原生插件，`alwaysOnBottom` 官方不支持（[issue #20223](https://github.com/electron/electron/issues/20223)） | 需 Rust `windows` crate 手写，可行但成本高 |
| Shell 图标 / .lnk | 现成库（Vanara / WindowsAPICodePack） | 需原生模块 | 需 Rust 手写 |
| OLE 拖放 | 原生支持 | 支持，但跨进程窗口细节多 | 需处理 |
| 体积 | 自包含单文件 ~60–80MB（可再精简） | ~100MB+ | ~几 MB |
| 开发效率（UI） | 中（XAML 数据绑定/动画完善） | 高（Web 栈） | 中 |
| 内存占用 | 低 | 高（每个渲染进程） | 低 |

**推荐：C# (.NET 8) + WPF**，理由：
1. 仅 Windows 场景下，原生栈对桌面层、Shell、拖放的接入最直接，没有 Electron/Tauri 的「桥接原生层」额外成本与不确定性；
2. WPF 自带数据绑定、`Storyboard` 折叠动画、透明/无边框窗口、`DoDragDrop`，完全够用；
3. 与主流开源同类方案一致，踩坑路径清晰；
4. 体积、内存都优于 Electron。

**备选**：若未来强烈希望用 Web 技术栈做 UI，可考虑 **Tauri 2**（Rust 后端 + Web 前端），但要接受 Rust 侧手写 Win32 交互的开发成本。

**关键依赖（NuGet）**：
- `Vanara.PInvoke` + `Vanara.Windows.Shell`：成熟的 P/Invoke 与 Shell 封装（WorkerW、`IShellLink` 解析 .lnk、`SHGetFileInfo`/`SHGetImageList` 取系统图标、右键原生菜单）。
- `CommunityToolkit.Mvvm`：盒子 UI 的 MVVM。
- `System.Text.Json`：配置持久化。

---

## 5. 总体架构

### 5.1 进程与窗口模型

- **单进程**（WPF 应用 + 一个 UI 线程 Dispatcher）。
- **每个盒子 = 一个独立 HwndSource 窗口**。正常情况下创建为由 WorkerW/Progman 所有的无边框逐像素透明窗口；无法取得桌面宿主时创建为无边框顶层窗口并置底。各盒子独立定位/折叠、互不影响。
- 进程常驻**系统托盘**：托盘菜单提供「新建盒子 / 锁定布局 / 设置 / 退出」等全局入口。
- 启动方式：开机自启（注册表 `Run`）+ 手动启动。

### 5.2 模块划分

```
src/
  DesktopBoxes.App/        # 入口、单例互斥、托盘、进程生命周期、开机自启
  DesktopBoxes.Core/       # 数据模型、配置持久化(JSON)、.lnk 解析、图标缓存、启动器
  DesktopBoxes.Win32/      # P/Invoke：WorkerW/Progman 贴桌面、SetWindowPos、DPI、Shell 图标、拖放目标
  DesktopBoxes.UI/         # 盒子窗口 XAML、图标项控件、标题栏、折叠动画、拖入/拖出、右键菜单
tests/
  DesktopBoxes.Tests/      # 单元测试（配置序列化、.lnk 解析、启动参数）
```

### 5.3 数据模型（核心）

```
Box（盒子）
├─ Id / Name / X / Y / Width / Height / Collapsed
├─ 外观（详见第 12 节，可空则继承全局主题）：
│    ThemePreset / BackgroundColor / BackgroundOpacity / CornerRadius
│    / BorderColor / BorderThickness / TitleBarBackground / TitleBarTextColor
│    / BackgroundImagePath / BackgroundImageMode（背景图片与显示方式）
└─ Items[]
      └─ Item（图标项）
          ├─ Path（绝对路径：.exe / .lnk / 文件夹 / 文件）
          ├─ Kind（Exe / Shortcut / Folder / File）
          ├─ DisplayName（可自定义，默认取文件名）
          └─ ResolvedTarget（.lnk 解析后的目标，用于启动与取图标）
```

数据文件统一存放于「数据目录」（默认 `%AppData%\DesktopBoxes\`），**变更防抖保存**（如 300ms）；启动时加载并还原全部盒子。数据目录位置可由用户配置，见第 5.4 节。

### 5.4 数据存储位置（可配置）

用户可决定数据（`boxes.json` + `backgrounds\` 背景图片 + `icon-cache\` 图标缓存）存放的位置，提供三种模式：

1. **默认模式**：`%AppData%\DesktopBoxes\`（普通安装用户的默认值）。
2. **自定义目录**：用户在设置里手动选择一个文件夹（如 D 盘或同步盘）。
3. **便携模式（可选）**：数据存到程序同目录的 `data\` 文件夹——当 exe 同目录存在 `portable` 空标记文件时自动启用，适合绿色版 / 放 U 盘。

**定位规则（启动时按顺序判断）：**
1. exe 同目录存在 `portable` 标记 → 用程序目录 `data\`（便携模式）
2. 默认位置存在指针文件 `storage.location`（内容为自定义目录绝对路径）→ 用该目录
3. 否则 → 默认 `%AppData%\DesktopBoxes\`

**切换位置时：**
- 把已有数据（`boxes.json`、`backgrounds\`、`icon-cache\`）整体迁移到新目录，再更新指针；
- 指针文件很小，只存一个路径，作为「默认位置 → 实际数据目录」的引导。

**失效降级：**
- 若自定义目录不可用（可移动磁盘被拔出 / 网络盘掉线），退回默认目录并给出提示，**不崩溃**；用户可重新指定。

---

## 6. 关键模块设计

### 6.1 贴桌面（见第 3 节）
- 主：创建窗口时把 `ParentWindow` 指向 WorkerW/Progman，并使用 `WS_POPUP` 保持顶层窗口能力；启用 `UsesPerPixelOpacity`，让盒子背景和标题栏半透明而图标、文字保持清晰。
- 兜底：置底 z-order + Show Desktop 保持 + 隐藏任务栏项。

### 6.2 图标渲染
- 通过 `SHGetImageList` 取系统大图标；`.lnk` 先经 `IShellLinkW + IPersistFile` 解析出目标路径再取图标。
- 图标做**内存缓存**（路径 → 图标句柄/位图），避免大量图标项时反复请求 Shell。

### 6.3 拖入（桌面/资源管理器 → 盒子）
- WPF `AllowDrop` + `DragEnter/Drop` 处理 `DataFormats.FileDrop`，得到文件路径数组 → 生成 `Item` 加入盒子并持久化。

### 6.4 拖出（盒子 → 桌面/其他盒子）
- 图标项上 `DoDragDrop`，以 `FileDrop` 格式携带路径，让桌面/其他盒子/资源管理器能接收。

### 6.5 启动应用 / 打开
- `.exe`：`Process.Start(path)`。
- `.lnk`：优先解析出目标 + 参数 + 工作目录后启动（保留快捷方式的运行参数）；兜底直接 `Process.Start(lnk路径)`（Windows 自行解析）。
- 文件夹：`explorer.exe <path>`；文件：按默认关联打开（`ShellExecute`）。

### 6.6 折叠 / 展开
- 折叠后窗口高度收缩为**标题栏**（约 28–32px），显示「盒子名 + 图标数量 + 展开/折叠箭头」。
- 用 WPF `Storyboard` 对窗口 `Height` 做动画过渡（内容区淡出/裁切）。
- 折叠状态持久化，重启后保持。

### 6.7 右键菜单
- **图标项**：打开 / 打开所在位置 / 从盒子移除（不删文件）/ 重命名 / 属性。
- **盒子空白区/标题栏**：折叠·展开 / 重命名盒子 / 自动排列图标 / 删除盒子 / 新建盒子 / 锁定布局。
- 优先复用 Windows 原生 Shell 菜单（`Vanara`），保持一致体验。

### 6.8 布局与多显示器 / DPI
- 应用标记 **Per-Monitor DPI Aware**，坐标在多显示器下正确映射；显示器拔插/分辨率变化时，把盒子约束回可见区域。

---

## 7. 功能清单与阶段划分

### 阶段 1（MVP，核心可用）
- [x] 托盘常驻 + 总控内开机自启开关 + 单实例互斥
- [x] 新建 / 删除 / 重命名盒子
- [x] 盒子贴桌面（WorkerW 主方案 + 置底兜底）
- [x] 拖入 .exe / .lnk / 文件夹 / 文件，显示图标与名称
- [x] 双击启动 / 打开
- [x] 图标项右键菜单（打开 / 打开位置 / 移除 / 重命名 / 属性）
- [x] 盒子折叠成标题栏 / 展开（含数量显示）
- [x] 盒子位置/大小/折叠状态持久化（JSON）
- [x] 拖出到桌面 / 资源管理器

### 阶段 2（体验打磨）
- [x] 折叠/展开动画
- [ ] 盒子外观自定义（已完成主题预设、颜色、背景图、图标大小和统一设置；边框等仍未完成）
- [ ] 盒子内自动排列 + 网格吸附 + 对齐参考线
- [ ] 多显示器 / 高 DPI 完整适配（已启用 Per-Monitor V2 和基础坐标换算，待多屏真机回归）
- [x] 图标项过多时的盒内滚动
- [ ] 盒子内搜索/过滤

### 阶段 3（可选，后续评估）
- [ ] 接管桌面模式（隐藏系统图标）
- [ ] 按规则自动归类桌面图标
- [ ] 文件夹门户

---

## 8. 技术风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| WorkerW/Progman 所有权在 Win10/Win11 各版本行为不一致 | 核心「贴桌面」失效 | 持续做真机验证；保留置底降级方案 |
| 挂接桌面层后 OLE 拖放目标行为异常 | 拖入/拖出失效 | spike 一并验证；必要时改用 `WM_DROPFILES` 接收拖放 |
| 高 DPI 多显示器坐标错位 | 盒子错位 | Per-Monitor DPI Aware + 统一坐标换算，纳入测试矩阵 |
| .lnk 解析失败（目标被移动/删除） | 图标/启动异常 | 解析失败回退为「直接按 .lnk 启动 + 通用图标」，不崩溃 |
| 杀软对 WorkerW / 原生窗口操作误报 | 安装/运行受阻 | 发布时做代码签名；文档说明 |
| 大量图标时卡顿 | 体验差 | 图标内存缓存 + 异步加载 + 盒内虚拟化 |

---

## 9. 开发里程碑

1. **M0 技术 spike（已完成）**：验证 WorkerW 挂接贴桌面 + 透明度 + 拖放 + 图标提取，锁定主/降级方案。
2. **M1 MVP（阶段 1）**：核心盒子交互闭环可用。
3. **M2 打磨（阶段 2）**：动画、主题、多显示器、自动排列。
4. **M3 打包发布**：单文件自包含 exe + 安装包（Inno Setup / MSIX）+ 代码签名。

---

## 10. 打包与发布

- 打包为 **.NET 8 自包含单文件**（免装运行时，双击即用，~60–80MB）；同时提供安装版（开机自启 + 开始菜单快捷方式）。
- 安装器：Inno Setup（轻量）或 MSIX（商店/签名友好）。
- 发布前做代码签名，降低杀软误报。

---

## 11. 评审结论（已确认）

1. **贴桌面层级**：盒子显示在系统桌面图标**之上**（盒子可盖住图标，符合「悬浮盒子」直觉）。
2. **盒子重叠**：允许盒子自由拖动、可互相覆盖。
3. **移除图标语义**：从盒子移除 = 仅移出盒子，**不删除磁盘文件**。
4. **图标大小**：图标尺寸可调（大 / 中 / 小）；盒子内网格对齐放阶段 2 打磨。
5. **语言**：UI 中文优先。
6. **外观自定义（换肤）**：需要，详细设计见第 12 节。
7. **数据存储位置可配置**：用户可选择数据存放目录（默认 / 自定义 / 便携），详见第 5.4 节。

---

## 12. 外观自定义（换肤）设计

### 目标
让盒子（主要是背景与标题栏）可自定义外观，既能融入壁纸，也能用不同颜色区分不同盒子。

### 可自定义项
1. **盒子背景**
   - 背景色：预设色板 + 自定义取色
   - 不透明度：新盒子默认 65%，滑杆支持 0%–100%
   - 圆角半径
   - 边框：颜色 / 粗细 / 显示或隐藏
   - **背景图片**：支持用户自选本地图片（jpg / png / webp / bmp）作为盒子背景
     - 显示方式：适应（等比缩放裁切，默认）/ 拉伸 / 平铺 / 居中
     - 图层叠加：图片在最底层，之上仍可叠加「颜色 + 不透明度」遮罩，保证图标文字可读
     - 图片复制到数据目录的 `backgrounds\` 子目录（数据目录位置可配置，见第 5.4 节），避免源文件被移动/删除后背景丢失
     - 可一键「还原为纯色背景」（移除图片）
2. **标题栏**
   - 背景色 / 字体颜色 / 字号
   - 是否显示「图标数量」徽标
   - 折叠态样式可与展开态独立
3. **图标与文字**
   - 图标大小（大 / 中 / 小，对应评审结论 4）
   - 文字颜色、是否加阴影 / 描边（提升在复杂壁纸上的可读性）
4. **主题预设**
   - 内置一键切换预设：浅色 / 深色 / 毛玻璃（Acrylic 亚克力）/ 极简（透明无边框）
   - 预设作为起点，改任意单项后自动标记为「自定义」
   - 毛玻璃通过 `SetWindowCompositionAttribute`（`ACCENT_ENABLE_ACRYLICBLURBEHIND`）实现，Win10/11 可用

### 作用范围（全局 vs 单盒）
- 提供「全局主题」：对所有盒子生效。
- 单个盒子可「覆盖全局」单独设置（per-box override），满足「不同盒子不同颜色」的整理需求。

### 数据模型补充
外观字段并入 `Box`（可空，空则继承全局主题，见第 5.3 节）。

### 入口与交互
- 盒子标题栏右键 →「盒子外观…」弹出设置面板（背景图片 / 背景色 / 透明度 / 圆角 / 边框 / 标题栏 / 图标文字）。
- 总控 →「统一外观」把当前外观应用到全部盒子。
- 设置实时预览、即时生效，并持久化到 `boxes.json`。

### 计划阶段
- 全部放入**阶段 2**；阶段 1 先使用一套默认外观（深色半透明 + 圆角）保证可用。

---

### 附：资料引用

- 开源 Fences 替代 desktop_box（.NET 8 + WPF）：https://github.com/kof2000git/desktop_box
- 开源 Fences 替代 openFrence：https://github.com/weiweigogo/openFrence
- Electron `alwaysOnBottom` 未支持：https://github.com/electron/electron/issues/20223
