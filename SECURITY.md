# 安全策略

## 支持范围

安全修复只针对最新发布版本和 `main` 分支。

## 报告漏洞

请不要在公开 Issue 中披露尚未修复的漏洞。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，并提供：

- 受影响版本和 Windows 版本；
- 可复现步骤与预期影响；
- 必要的日志或最小复现文件（请移除个人路径和敏感数据）。

如果仓库尚未启用 Private vulnerability reporting，请先只提交一个不包含漏洞细节的 Issue，请求维护者提供私密联系方式。
