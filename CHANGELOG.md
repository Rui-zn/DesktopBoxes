# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并使用语义化版本号。

## [Unreleased]

## [1.2.0] - 2026-09-10

### Changed

- 文件拖放由外部引用改为双向移动；支持桌面 / 资源管理器与盒子往返、跨盘文件夹及盒子间移动。
- 删除盒子和右键“移回桌面”会安全移出托管文件；同名项目不直接覆盖，旧版引用不会在升级时自动移动。
- 正常退出或捕获到致命异常时，按盒子名称将内容安全复制到桌面文件夹；已有同名内容不会被覆盖。
- 再次启动时通过安全标记和 SHA-256 清单移除未修改的退出副本；用户修改或新增的桌面内容保持原样。
- 将“开机自动启动”从托盘菜单移到总控侧栏，并采用新的多尺寸桌面盒子应用图标。
- 新盒子默认使用 65% 不透明度；桌面模式改为 WorkerW/Progman 所有的逐像素透明窗口，使背景和标题栏透明且图标、文字保持清晰。

- 重新设计总控：浅色管理面板、盒子卡片、名称搜索、项目统计、选中盒子的预览与快捷操作。
- 重新设计外观编辑器：午夜 / 云白 / 松林 / 暮紫主题、选中标记、实时预览、可滚动设置与固定保存栏。
- 桌面盒子采用圆角卡片、数量徽标和柔和图标底板；标题栏常驻可交互的独立锁按钮，提供悬停、按下和键盘焦点反馈。
- WorkerW/Progman 桌面挂接与顶层兼容模式均使用圆角，折叠、缩放和 DPI 变化时同步裁剪；旧直角配置自动显示为默认圆角。
- 每盒锁按钮可固定 / 解锁位置和大小，折叠后仍可操作；外观编辑器在桌面模式下也提供圆角选择。
- 统一按钮、输入框、滚动条、重命名对话框与空状态；保留已有的自定义外观和 WorkerW 桌面挂接方式。

### Fixed

- 混合锁定状态下“锁定全部”现在正确锁定所有盒子；空名称无法保存。
- 背景图片无法解码时回退到背景颜色；选图时即时校验，取消编辑不复制图片或改写配置。
- 快速反向折叠从当前高度继续动画；锁定时取消正在进行的移动 / 缩放。

### Added

- 托管文件目录、同步保存、移动恢复记录和托管项目扫描恢复；数据目录迁移包含真实文件及路径重定位。
- 文件移动单元测试与隔离的 Explorer OLE / 跨磁盘往返检查，不操作用户桌面文件。

- 独立 WPF UI 验证工具，使用隔离数据检查交互并生成界面截图。
- GitHub Actions 构建、测试和发布流程。
- 贡献指南、安全策略、编辑器配置和 MIT License。

## [1.1.0] - 2026-09-07

### Added

- WorkerW/Progman 桌面嵌入及 Explorer 重启后自动重挂。
- Per-Monitor V2 DPI 支持和基础坐标换算。
- 配置原子保存、备份恢复和损坏文件保留。
- 数据目录迁移、主题预设和文件属性入口。
- 自包含单文件发布和正确的便携模式标记。

### Changed

- 保留 Windows 系统桌面图标，不再默认接管桌面。

### Fixed

- 第二实例退出可能改变桌面图标状态的问题。
- 配置损坏后被空配置覆盖的问题。
- 图标轻微移动即触发拖放及背景图片取消后遗留文件的问题。
